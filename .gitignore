# Entornos virtuales
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Datos reales — NUNCA al repo (regla de privacidad LOPDP del handoff)
*.xlsx
*.csv
*.parquet
# Listados de RUC extraidos del registro. Son dato publico, pero un extracto de 221.794
# empresas en el repo es un fichero de datos, no codigo, y aqui no pinta nada.
demo/rucs_*.txt
data/
clean/
cuarentena/

# Secretos / credenciales
.env
*.json
# intermedios de experimentos (Zref y compania): centenares de MB
*.npy
# ...pero el catalogo CIIU SI se versiona: es estandar publico, 49 KB, y el servicio no
# arranca sin el. Con `*.json` a secas se quedaba fuera y rompia en cualquier otra
# maquina sin que nadie lo notara aqui.
!src/benchmarking/datos/*.json
# log del uvicorn local
uvicorn.log
!**/esquema_plantilla.yaml

# SO
Thumbs.db
.DS_Store

# artefactos de build (pip wheel / setuptools)
build/
dist/
*.egg-info/

# log de la corrida local del universo
corrida_universo*.log

# descargas sueltas de investigacion
*.html
.gstack/

# caches de embeddings: 150 MB de vectores derivados, se regeneran solos
*.npz
