# Cloud Build: construye la imagen del pipeline desde Dockerfile.pipeline.
# Uso: gcloud builds submit --config infra/cloudbuild.pipeline.yaml --substitutions=_IMAGE=<img> .
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "Dockerfile.pipeline", "-t", "${_IMAGE}", "."]
images: ["${_IMAGE}"]
options:
  logging: CLOUD_LOGGING_ONLY
